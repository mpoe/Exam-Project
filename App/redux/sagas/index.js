import { fork } from 'redux-saga/effects';
import * as player from './playerAdmin';

//export root saga function
export default function* root() {
    //yield fork (player.updatePlayerPositionHandler)
}